import React from 'react';
import { shallow } from 'enzyme';
import LoginCard from './LoginCard';

describe('Test for login card', () => {
    it('should give true', () => {
        expect(true).toBe(true);
    });
});
