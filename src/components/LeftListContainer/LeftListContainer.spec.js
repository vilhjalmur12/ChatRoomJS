import React from 'react';
import { shallow } from 'enzyme';
import LeftListContainer from './LeftListContainer';

describe('check for left list container', () => {
    it('should give true', () => {
        expect(true).toBe(true);
    });
});
